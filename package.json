{
  "name": "portfolio",
  "description": "Mono repo of portfolio webapp and serverless functions",
  "author": "John Schellinger",
  "license": "MIT",
  "scripts": {
    "postinstall": "cd app && npm install && cd ../cloud && npm install",
    "test": "echo 1; No tests are defined"
  },
  "devDependencies": {
    "serverless": "^3.39.0",
    "serverless-compose": "^2.4.0"
  }
}
